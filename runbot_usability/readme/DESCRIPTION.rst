This module is an extra module that adds extra features regarding runbot
and also demo data.

Add the possibility to customize root path, in the odoo config file.

``runbot_root_path = /my/custom/root/path``
